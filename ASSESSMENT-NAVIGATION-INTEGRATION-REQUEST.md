# Navigation integration record

The assessment schedule is available at `assessment.html`, linked from `resources.html`, and included in the shared navigation as `Assessment`.

The shared navigation generator in `assets/site.js` now supplies the route correctly from root and nested pages, with `data-page="assessment"` setting the active destination.

No push or deployment has been performed.
